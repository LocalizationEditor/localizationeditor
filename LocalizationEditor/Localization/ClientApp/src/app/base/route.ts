export class CustomRoute {
  id: number;
  name: string;
  route: string;

  constructor(id: number, name: string, route: string) {
    this.id = id;
    this.name = name;
    this.route = route;
  }
}
